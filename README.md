## Project setup
1) copy paste .env.example into new file called .env
2) check notion for necessery env data


```bash
$ npm install
```

## init docker

```bash
# init
$ docker-compose up

# in case of detachment 
$ docker-compose up -d

# testing build
$ docker-compose up --build
```

